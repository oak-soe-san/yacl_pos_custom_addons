<odoo>
    <record id="view_res_partner_form" model="ir.ui.view">
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page name="page_passport" string="Passport">
                    <group>
                        <group>
                            <field name="surname" string="Surname" />
                            <field name="forenames" string="Forenames" />
                            <field name="date_of_birth" string="Date of Birth" />
                            <field name="expiry_date" string="Expiry Date" />
                            <field name="issue_date" string="Issue Date" />
                            <field name="document" string="Document" />
                            <field name="doc_number" string="Doc. Number" />
                            <field name="issuing_state" string="Issuing State" />
                        </group>
                        <group>
                            <field name="nationality" string="Nationality" />
                            <field name="flight_number" string="Flight Number" />
                            <field name="flight_date" string="Flight Date" />
                            <field name="height" string="Height" />
                            <field name="weight" string="Weight" />
                            <field name="hair_color" string="Hair Color" />
                            <field name="eye_color" string="Eye Color" />
                            <field name="gender" string="Gender" />
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
